package com.exchnage.demo.processor;

import org.springframework.stereotype.Service;

@Service
public class DataProcessor {
//       public List<List<String >> sortList(String Type, List<List<String>> bids){
//           bids.stream().sorted(Comparator.comparing((List<String> first, List<String>second)-> Long.parseLong(first.get(0)) > Long.parseLong(second.get(0))))
//        }
}
